<h1>burgers!</h1>

<h2>burgers that are not devoured!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/cat-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>burgers that are devouredy</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/cat-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devouredy">devouredy?</label><br>
    <input type="radio" name="devouredy" value="1" checked> devoured!<br>
    <input type="radio" name="devouredy" value="0"> Awake
  </div>

  <button type="submit">Add Burger</button>
</form>
